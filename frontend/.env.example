# VITE_API_URL=http://127.0.0.1:8000
#
# Leave unset in development to route API calls through the Vite dev proxy
# (see vite.config.ts), which avoids CORS.
#
# Set to the backend origin for production builds, e.g.:
# VITE_API_URL=https://swimlane.example.com