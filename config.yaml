logging:
  level: INFO  # Override with LOG_LEVEL env var
  # file: logs/app.log  # Optional alternative to LOG_FILE env var

security:
  algorithm:
    HS256
  access_token_expire_minutes:
    15
  refresh_token_expire_days:
    7
  # SPA origin: the OAuth callback redirects here with the local JWTs appended.
  # Override with the FRONTEND_URL env var.
  frontend_url:
    http://localhost:5173
  web_admins:
    # pete@letkeman.ca = 111122296393493391055
    - 111122296393493391055
    - sub2

sql:
  active:
    sqlite
    # postgresql

  providers:
    sqlite:
      # sqlite specific config
      sqlite_file:
        swimlane.db

    postgresql:
      # postgresql specific config values, not credentails
